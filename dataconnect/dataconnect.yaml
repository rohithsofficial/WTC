specVersion: "v1beta"
serviceId: "wtc-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "wtc-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
